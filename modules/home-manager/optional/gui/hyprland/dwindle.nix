{...}: {
  wayland.windowManager.hyprland.settings.dwindle = {
    pseudotile = "true"; # enable mid-tree window floating (pseudo)
    preserve_split = "true"; # so we can switch the split direction
  };
}
